# Utilities for Image Processing and Result Formatting

## Image Processing Utilities

### Function: preprocess_image(image)
Description: Prepares an image for analysis by resizing and normalizing the pixel values.

### Function: apply_filter(image, filter_type)
Description: Applies a specified filter to the image (e.g., Gaussian, Canny).

## Result Formatting Utilities  

### Function: format_results(detection_results)
Description: Formats the results of the detection into a user-friendly string or a structured format.

### Function: save_results_to_file(results, file_path)
Description: Saves the formatted results to a specified file path.
